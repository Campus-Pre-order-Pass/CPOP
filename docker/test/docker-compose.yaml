version: "3"

services:
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"

  rabbitmq:
    # image: rabbitmq:latest
    image: rabbitmq:3.12.6-management
    environment:
      RABBITMQ_DEFAULT_USER: celery
      RABBITMQ_DEFAULT_PASS: password123
      RABBITMQ_DEFAULT_VHOST: my_vhost
    ports:
      - "5672:5672"
      - "15672:15672"

  # celery:
  #   build: .
  #   container_name: cpop-server-celery
  #   command: cd ../../Server && celery -A Backend worker -l info

  # flower:
  #   build: .
  #   container_name: cpop-server-flower
  #   command: cd ../..//Server && celery -A Backend flower -l info --basic_auth=twtrubiks:password123
  #   ports:
  #     - "5555:5555"
